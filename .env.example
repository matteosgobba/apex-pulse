# Optional override used when commands are run outside the repository root.
# F1_PREDICTION_PROJECT_ROOT=/absolute/path/to/repository

# FastF1 cache and output paths are configured in configs/data.yaml.
